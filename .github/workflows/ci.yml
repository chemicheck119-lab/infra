name: Infrastructure CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  config-check:
    name: config-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: scripts/test_config.sh
